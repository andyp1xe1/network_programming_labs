[project]
name = "lab2"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Andre Chicu", email = "andriesh.chicu@gmail.com" }
]
requires-python = ">=3.13"
dependencies = [
    "requests",
]

[project.scripts]
lab2 = "lab2:main"
server-concurrent = "lab2.server:main"
test-server = "lab2.test_server:main"

[build-system]
requires = ["uv_build>=0.8.17,<0.9.0"]
build-backend = "uv_build"
